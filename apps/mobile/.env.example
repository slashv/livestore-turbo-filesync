# Copy this file to .env for local development
#
# NOTE: For physical devices, replace 'localhost' with your machine's IP address
# Example: LIVESTORE_SYNC_URL=http://192.168.1.100:8787/sync

# LiveStore sync server URL (used by app.config.ts)
LIVESTORE_SYNC_URL=http://localhost:8787/sync

# API URL for authentication (defaults to sync URL without /sync)
API_URL=http://localhost:8787
