{
  "name": "@repo/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist .vite",
    "dev": "vite",
    "build": "tsc && vite build",
    "build:preview": "tsc && vite build --mode preview",
    "build:production": "tsc && vite build --mode production",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test:e2e": "playwright test --config=e2e/playwright.config.ts",
    "test:e2e:ui": "playwright test --config=e2e/playwright.config.ts --ui",
    "deploy:preview": "pnpm build:preview && wrangler pages deploy dist --project-name=livestore-filesync-gallery",
    "deploy:prod": "pnpm build:production && wrangler pages deploy dist --project-name=livestore-filesync-gallery --branch production"
  },
  "dependencies": {
    "@livestore-filesync/core": "workspace:*",
    "@livestore-filesync/image": "workspace:*",
    "@livestore-filesync/opfs": "workspace:*",
    "@livestore/adapter-web": "catalog:",
    "@livestore/livestore": "catalog:",
    "@livestore/peer-deps": "catalog:",
    "@livestore/react": "catalog:",
    "@livestore/sync-cf": "catalog:",
    "@livestore/wa-sqlite": "catalog:",
    "@repo/store": "workspace:*",
    "@repo/ui": "workspace:*",
    "better-auth": "^1.2.0",
    "effect": "catalog:",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@livestore/devtools-vite": "catalog:",
    "@playwright/test": "^1.48.0",
    "@repo/tsconfig": "workspace:*",
    "@tailwindcss/vite": "^4.1.18",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.0",
    "postcss": "^8.4.49",
    "tailwindcss": "^4.1.18",
    "typescript": "^5.7.2",
    "vite": "^6.0.0",
    "wrangler": "^4.59.1"
  }
}
